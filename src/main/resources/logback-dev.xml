<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <include resource="logback-spring.xml"/>

    <!-- Root logger for dev -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
